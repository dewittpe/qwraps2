# Check Comments

A more robust check for open/close matching sets of comments in a spin
file.

## Usage

``` r
check_comments(c1, c2)
```

## Arguments

- c1:

  index (line numbers) for the start delimiter of comments

- c2:

  index (line numbers) for the end delimiter of comments
